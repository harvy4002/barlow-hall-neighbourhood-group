name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:
  # The sync-wiki workflow commits with the default GITHUB_TOKEN, and GitHub
  # doesn't let GITHUB_TOKEN-authored pushes trigger other workflows (an
  # anti-recursion safeguard) — so an hourly sync's `push` event above never
  # fires. This is what actually redeploys the site after a sync commit.
  workflow_run:
    workflows: ["Sync Wiki from Outline"]
    types: [completed]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    # Skip if the triggering sync run failed or was cancelled — only a
    # successful sync (or a direct push/manual dispatch) should redeploy.
    if: github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist/

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/deploy-pages@v4
        id: deployment
